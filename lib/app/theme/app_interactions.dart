import 'package:flutter/material.dart';

class AppInteractions {
  const AppInteractions._();

  static const double pressedScale = 0.99;
  static const Duration pressedDuration = Duration(milliseconds: 100);
}
